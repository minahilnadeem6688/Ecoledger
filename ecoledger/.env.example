# Copy to .env and adjust. Never commit the real .env.
MONGO_URI=mongodb://127.0.0.1:27017/ecoledger
PORT=5000
JWT_SECRET=change-this-to-a-long-random-string

# First admin account, created on first start
ADMIN_EMAIL=admin@ecoledger.dev
ADMIN_PASSWORD=admin123

# Local Hardhat chain. OWNER_PRIVATE_KEY defaults to Hardhat test account #0 (dev only).
RPC_URL=http://127.0.0.1:8545
# CONTRACT_ADDRESS=   (optional: otherwise read from deployments/localhost.json after "npm run deploy")
# OWNER_PRIVATE_KEY=
